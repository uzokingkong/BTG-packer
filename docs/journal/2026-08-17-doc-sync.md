# 2026-08-17 — README / 기술 문서 분석·갱신 (코드 기준 동기화)

## 작업
`C:\Users\uzoki\Desktop\asdfsadfecwecc` (btg-packer) 의 `src/` 전반을 분석하고,
실제 코드와 일치하지 않는 문서 지점을 수정했다. **빌드/테스트는 실행하지 않았다**
(동시에 다른 에이전트가 `src/` 코드를 수정 중이므로 소스는 건드리지 않음).

## 변경 (문서만, 커밋 `506b825`)
- `README.md` — 프로그램 VM(`--vm-oep`)의 네이티브 제외 집합
  (panic/unwind/Once, setjmp/longjmp, SEH catch/unwind) 및
  `BTG_SEH_MINIMAL` / `BTG_SEH_NONE` 환경변수 토글을 "구현된 것"과
  "알려진 한계"에 반영. v56 LOCK-RMW 격리 망 제거 사실도 명시.
- `docs/architecture/coverage.md` — opcode 계수 표기 정정
  (`NUM_OPS=0xC2`, 0x01..=0xC1), SHLD/SHRD·DF(CLD/STD) 추가,
  "네이티브 제외" 섹션을 `exclusions.rs`의 실제 세 함수
  (`detect_panic_unwind_ranges`, `detect_setjmp_longjmp_functions`,
  `detect_seh_native_functions`) 기준으로 재작성, v56 LOCK-RMW 격리 망 제거 명시.
- `docs/architecture/actual-pipeline.md` — 2.4.1 네이티브 유지 집합 섹션 추가,
  at-rest 암호화 시 ASLR 비활성화 명시, `NUM_OPS` 표기 정정.

## 미완료 / 주의
- `src/vm/text_lift/exclusions.rs` 는 다른 에이전트가 수정 중(작업 트리에 변경
  존재). 커밋에서 제외했으며, 해당 작업이 끝나면 별도 커밋 필요.
