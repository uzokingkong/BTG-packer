# Journal 2026-08-14 - T1-4 interp coverage + T1-3 native codegen (abda066 to 607f655)  
  
Completed full 12-op coverage in PolymorphicInterpreter, RiscProgram::eval_state reference simulator (T1-4 differential contract), and all-12 native handler codegen in DirectThreadedNativeRunner. cargo test 118 pass. Smoke pack OK. Pushed to origin/main.  

## T1-2 — RISC lifter coverage extension (87703a0 → cea9e5a)

Extended the commercial RISC lifter to cover CALL (direct/indirect), the full Jcc set, CMP, memory-operand arithmetic (Add/Sub/Xor/And/Or read-modify-write), SHL/SHR shifts, MOVZX, and LEAVE. Completed `RiscProgram::eval_state` to simulate `VirtualBranch` + `MemoryRead`/`MemoryWrite` (VIP loop + byte-granular memory model). Full `cargo test --release`: **136 passed** (126 baseline + 10 new). Commits `87703a0` (code) & `cea9e5a` (doc) pushed to origin/main. Full details: `docs/T1-2-RISC-Lifter-Coverage-DONE.md`.
