# Journal 2026-08-14 - T1-4 interp coverage + T1-3 native codegen (abda066 to 607f655)  
  
Completed full 12-op coverage in PolymorphicInterpreter, RiscProgram::eval_state reference simulator (T1-4 differential contract), and all-12 native handler codegen in DirectThreadedNativeRunner. cargo test 118 pass. Smoke pack OK. Pushed to origin/main.  

## T1-2 — RISC lifter coverage extension (87703a0 → cea9e5a)

Extended the commercial RISC lifter to cover CALL (direct/indirect), the full Jcc set, CMP, memory-operand arithmetic (Add/Sub/Xor/And/Or read-modify-write), SHL/SHR shifts, MOVZX, and LEAVE. Completed `RiscProgram::eval_state` to simulate `VirtualBranch` + `MemoryRead`/`MemoryWrite` (VIP loop + byte-granular memory model). Full `cargo test --release`: **136 passed** (126 baseline + 10 new). Commits `87703a0` (code) & `cea9e5a` (doc) pushed to origin/main. Full details: `docs/engine/T1-2-RISC-Lifter-Coverage-DONE.md`.

## T1-2 잔여분 — SAR/MOVSX/JP-JNP/Jcxz-Jecxz/unsigned Jcc 정밀화 (b6991dd → 5d3867d)

이전 커밋(87703a0)에서 "미지원"으로 남아있던 T1-2 RISC 리프터 커버리지 격차를 구현·검증·커밋·푸시했다.

- **SAR**: `RiscOp::ArithmeticShiftRight` 신규 op + `eval_state`(부호 유지 우측 시프트) + `Sar_rm*` 리프트.
- **MOVSX**: `(src << (64-w)) >> (64-w)` 산술 시프트로 부호 확장 (8/16/32-bit, `Movsx_*`/`Movsxd_*`).
- **JP/JNP**: `VirtualFlags`가 low-byte 짝수 패리티 PF 계산; `BranchCondition::Parity/NotParity`.
  네이티브 하네스 `FLAG_MASK` 0x8C1→0x8C5, add-path 0xC4로 동기화(참조↔네이티브 차등 유지).
- **Jcxz/Jecxz/Jrcxz**: `BranchCondition::CounterZero(width)` — regs[1](RCX) 하위 2/4/8바이트==0.
- **Ja/Jae/Jb/Jbe**: `NotCarry`/`Carry` 근사 → `Above`(CF=0∧ZF=0)/`AboveOrEqual`(CF=0)/
  `Below`(CF=1)/`BelowOrEqual`(CF=1∨ZF=1) 정밀 분리.

신규 테스트 6개(sar, movsx, jp, jrcxz, ja/jae, jbe). 전체 테스트는 crypto 네이티브-exec SIGSEGV(사전 존재)를
제외하고 141 passed / 0 failed. 커밋: `b6991dd`(code) + `5d3867d`(doc), origin/main 푸시됨.
